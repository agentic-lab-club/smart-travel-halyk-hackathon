package moduletemplate

// Add focused `*_test.go` tests for new endpoints.
//
// Suggestions:
// - Handler tests: create a `fiber.New()` app, register routes, and hit it with requests.
// - Service tests: use a fake repository interface (if you define one) or a test DB setup used by existing modules.
//
// Keep tests table-driven and assert HTTP status + response shape.

